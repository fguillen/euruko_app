<p>
  <ul>
      <% paper.speakers.each do |speaker| %>
        <li>
          <%= link_to speaker.user.name, speaker.user %>
          <%= link_to 'delete', paper_speaker_path( paper, speaker ), :method => 'delete' %>
        </li>
      <% end %>
  </ul>
</p>
<p>
  <% form_for [paper, Speaker.new] do |f| -%>
    <%=(
      select(
        "speaker", 
        "user_id", 
        User.find_public.collect{|u| [ u.name, u.id ] }, 
        { :include_blank => true }
      )
    )%>
    <%= f.submit "Add Speaker" %>
  <% end -%>
</p>